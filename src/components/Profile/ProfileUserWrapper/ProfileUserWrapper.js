import { ProfileUserWrapperStyle } from './ProfileUserWrapper.styled';

export const ProfileUserWrapper = ({ children }) => {
  return <ProfileUserWrapperStyle>{children}</ProfileUserWrapperStyle>;
};
